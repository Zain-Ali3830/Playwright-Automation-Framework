# # name: Playwright Tests

# # on:
# #   push:
# #     branches: [main, master]
# #   pull_request:
# #     branches: [main, master]

# # jobs:
# #   test:
# #     timeout-minutes: 60
# #     runs-on: ubuntu-latest
# #     container:
# #       image: mcr.microsoft.com/playwright:v1.47.0-jammy # 👈 Playwright Docker image (browsers preinstalled)

# #     steps:
# #       - uses: actions/checkout@v4

# #       - uses: actions/setup-node@v4
# #         with:
# #           node-version: lts/*

# #       - name: Install dependencies
# #         run: npm ci

# #       # 👇 Browsers already included in Docker image, so this step is removed
# #       # - name: Install Playwright Browsers
# #       #   run: npx playwright install --with-deps

# #       - name: Run Playwright tests
# #         run: npx playwright test

# #       - name: Upload Playwright report
# #         if: ${{ !cancelled() }}
# #         uses: actions/upload-artifact@v4
# #         with:
# #           name: playwright-report
# #           path: playwright-report/
# #           retention-days: 30







name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    container:
      # CHANGED: Using a custom image where dependencies are pre-baked
      image: ghcr.io/Zain-Ali3830/playwright-tests:latest 

    steps:
      - uses: actions/checkout@v4
      # ❌ REMOVED: actions/setup-node is not needed as Node is in the custom container
      # ❌ REMOVED: Caching steps are not needed as dependencies are not installed dynamically
      # ❌ REMOVED: Install dependencies (npm ci) is not needed as dependencies are pre-installed in the container
      
      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30















































name: Playwright Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    container:
      # CHANGED: Using a custom image where dependencies are pre-baked
      image: ghcr.io/your-org/playwright-tests:latest 

    steps:
      - uses: actions/checkout@v4

      # ❌ REMOVED: actions/setup-node is not needed as Node is in the custom container
      # ❌ REMOVED: Caching steps are not needed as dependencies are not installed dynamically
      # ❌ REMOVED: Install dependencies (npm ci) is not needed as dependencies are pre-installed in the container
      
      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30